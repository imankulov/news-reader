[project]
name = "news-reader-pydantic-ai"
version = "0.1.0"
description = "News reader using Pydantic AI with sub-agents and structured output"
requires-python = ">=3.12"
dependencies = [
    "pydantic-ai-slim[anthropic]",
    "httpx",
    "markdownify",
]
